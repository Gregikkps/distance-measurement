import 'package:flutter/material.dart';

class AppDistance {
  static const smallPadding = EdgeInsets.all(10.0);
  static const standardPadding = EdgeInsets.all(20.0);
  static const buttonTextPadding = EdgeInsets.all(10.0);
}
